<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/bottom_sheet_behavior"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Restor">
        <activity
            android:name=".PizzaAdapter"
            android:exported="false"
            tools:ignore="Instantiatable" />
        <!-- ДОБАВЬ ЭТУ СТРОКУ НИЖЕ -->
        <activity
            android:name=".PizzaDetailActivity"
            android:exported="false" />
        <activity
            android:name=".zakazpizzaActivity"
            android:exported="false" />
        <activity
            android:name=".VanyaActivity"
            android:exported="false" />
        <activity
            android:name=".Arbatactivity"
            android:exported="false" />
        <activity
            android:name=".gogolactivity"
            android:exported="false" />
        <activity
            android:name=".dodoactivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>